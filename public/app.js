const API=window.__HATCHABLE__?.api||'/api';const $=id=>document.getElementById(id);let history=[];const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));const pct=v=>`${Math.round((Number(v)||0)*100)}%`;function dataURL(file){return new Promise((ok,no)=>{const r=new FileReader();r.onload=()=>ok(r.result);r.onerror=no;r.readAsDataURL(file)})}async function analyze(file){$('fileNote').textContent=file.name;$('preview').src=await dataURL(file);$('emptyState').classList.add('hidden');$('resultState').classList.add('hidden');$('stage').textContent='ANALYZING';const fd=new FormData();fd.append('file',file);try{const res=await fetch(API+'/v1/analyze',{method:'POST',body:fd});const d=await res.json().catch(()=>({error:'Invalid server response'}));if(!res.ok)throw new Error(d.error||'Analysis failed');render(d);loadHistory()}catch(e){$('stage').textContent='ERROR';$('emptyState').classList.remove('hidden');$('emptyState').innerHTML=`<div class="empty-icon">!</div><h3>Analysis failed</h3><p>${esc(e.message)}</p>`}}function render(d){$('stage').textContent='COMPLETE';$('resultState').classList.remove('hidden');$('scoreValue').textContent=d.quality_score;$('qualityLabel').textContent=d.quality_label;$('decisionLine').textContent=d.decision_summary;$('issues').innerHTML=(d.issues||[]).map(i=>`<div class="issue"><span class="issue-name">${esc(i.type.replaceAll('_',' ').toUpperCase())}</span><span class="pill">${esc(i.severity.toUpperCase())}</span><span class="confidence">${pct(i.confidence)}</span></div>`).join('')||'<div class="issue"><span class="issue-name">NO MATERIAL ISSUES</span><span class="pill">LOW RISK</span><span class="confidence">—</span></div>';$('stats').innerHTML=Object.entries(d.statistics||{}).map(([k,v])=>`<div class="stat"><span class="k">${esc(k.replaceAll('_',' ').toUpperCase())}</span><span class="v">${esc(typeof v==='number'?Number(v).toFixed(3):v)}</span></div>`).join('');$('explanation').innerHTML=(d.explanation||[]).map(x=>`<div class="explain-line">${esc(x)}</div>`).join('');$('inference').textContent=`INFERENCE ${d.processing_time_ms} MS`;$('confidence').textContent=`CONFIDENCE ${pct(d.confidence)}`;$('uncertainty').textContent=`UNCERTAINTY ${esc(d.uncertainty_label)}`;$('heatmap').src=d.heatmap_url;$('heatmap').classList.add('hidden');$('heatBtn').textContent='SHOW HEATMAP'}async function loadHistory(){const r=await fetch(API+'/v1/analyses');if(!r.ok)return;const d=await r.json();history=d.items||[];renderHistory(document.querySelector('.filter.active')?.dataset.filter||'ALL')}function renderHistory(f){const rows=history.filter(x=>f==='ALL'||x.quality_label===f);$('historyTable').innerHTML=rows.length?`<div class="row header"><div>IMAGE</div><div>SCORE</div><div>LABEL</div><div>TOP ISSUE</div><div>DATE</div></div>`+rows.map(x=>`<div class="row"><div title="${esc(x.filename)}">${esc(x.filename)}</div><div><strong>${x.quality_score}</strong></div><div>${esc(x.quality_label)}</div><div>—</div><div>${new Date(x.created_at).toLocaleDateString()}</div></div>`).join(''):'<div class="empty-table">No analyses match this filter.</div>'}$('chooseBtn').onclick=()=>$('fileInput').click();$('fileInput').onchange=e=>{const f=e.target.files?.[0];if(f)analyze(f)};['dragenter','dragover'].forEach(ev=>$('uploadCard').addEventListener(ev,e=>{e.preventDefault();$('uploadCard').classList.add('drag')}));['dragleave','drop'].forEach(ev=>$('uploadCard').addEventListener(ev,e=>{e.preventDefault();$('uploadCard').classList.remove('drag')}));$('uploadCard').addEventListener('drop',e=>{const f=e.dataTransfer.files?.[0];if(f)analyze(f)});$('heatBtn').onclick=()=>{$('heatmap').classList.toggle('hidden');$('heatBtn').textContent=$('heatmap').classList.contains('hidden')?'SHOW HEATMAP':'HIDE HEATMAP'};$('opacity').oninput=e=>{$('opacityValue').textContent=e.target.value+'%';$('heatmap').style.opacity=Number(e.target.value)/100};document.querySelectorAll('.filter').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter').forEach(x=>x.classList.remove('active'));b.classList.add('active');renderHistory(b.dataset.filter)});loadHistory();