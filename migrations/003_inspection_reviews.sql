CREATE TABLE inspection_reviews (id uuid PRIMARY KEY DEFAULT gen_random_uuid(), analysis_id uuid NOT NULL REFERENCES analyses(id) ON DELETE CASCADE, status text NOT NULL DEFAULT 'unreviewed', reason text, note text, correctness text, incorrect_area text, corrective_action text, reviewer text, created_at timestamptz NOT NULL DEFAULT now(), updated_at timestamptz NOT NULL DEFAULT now(), UNIQUE (analysis_id));