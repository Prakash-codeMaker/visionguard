export const access='public';
import {db} from 'hatchable';
export const methods=['GET'];
export default async function(req,res){try{const {rows}=await db.query("SELECT a.id,a.inspection_number,a.created_at,a.filename,a.quality_score,a.quality_label,a.confidence,a.uncertainty,a.model_version,a.processing_time_ms,coalesce(r.status,'unreviewed') review_status,CASE WHEN a.defect_score>=0.5 THEN 'potential visual defect' WHEN a.corruption_score>=0.5 THEN 'severe degradation' WHEN a.blur_score>=0.5 THEN 'blur' WHEN a.underexposure_score>=0.5 THEN 'underexposure' WHEN a.overexposure_score>=0.5 THEN 'overexposure' WHEN a.noise_score>=0.5 THEN 'noise' ELSE '—' END AS top_issue FROM analyses a LEFT JOIN inspection_reviews r ON r.analysis_id=a.id ORDER BY a.created_at DESC LIMIT 100");res.json({items:rows})}catch(err){console.log(JSON.stringify({event:'history_failed',message:err?.message||String(err)}));res.status(500).json({error:{code:'HISTORY_ERROR',message:'Could not load inspection history.'}})}}